# crear el runner 
# crear llaves asimetricas en la instancia 1
ssh-keygen -t rsa -b 4096 -C "github-actions" -f ~/.ssh/id_rsa

# mostrar la llave publica en primera instancia
cat ~/.ssh/id_rsa.pub

# copiar la llave publica de las llaves asimetricas en la segunda instancias 

echo "paste-your-public-key-content-here" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
chmod 700 ~/.ssh

#regresar los permisos 

chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys

# probar en la primer instancia

# agregar la llave privada en las secret de github y ip de la segunda instancia 